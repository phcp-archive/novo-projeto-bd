<div class="conteudo">
<h1>Fornecedores</h1>

<div class="data">
<!--Form de busca-->
<%= form_tag fornecedores_path, :method => 'get'  do %>
   <%= text_field_tag :search, params[:search]%>
   <%= submit_tag "Buscar", :nome => nil  %>
 <% end %>
<!--/Form de busca-->
</div>


<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Cnpj</th>
      <th>Email</th>
      <th>Endereço</th>
      <th>Telefone</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @fornecedores.each do |fornecedor| %>
      <tr>
        <td><%= fornecedor.nome %></td>
        <td><%= fornecedor.cnpj %></td>
        <td><%= fornecedor.email %></td>
        <td><%= fornecedor.endereco.logradouro %>, <%= fornecedor.endereco.numero %>, <%= fornecedor.endereco.cidade %>, <%= fornecedor.endereco.estado %></td>
        <td>(<%= fornecedor.telefone.ddd %>) <%= fornecedor.telefone.numero %></td>
        <td><%= link_to 'Show', fornecedor %></td>
        <td><%= link_to 'Edit', edit_fornecedor_path(fornecedor) %></td>
        <td><%= link_to 'Destroy', fornecedor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Fornecedor', new_fornecedor_path %>
</div>